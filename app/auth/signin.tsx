import { SafeAreaView, Text } from 'react-native';

export default function SigninScreen() {
    return (
        <SafeAreaView className="flex-1 bg-neutral-50">
            <Text className="text-4xl font-extrabold text-neutral-900 mt-8">Signin</Text>
        </SafeAreaView>
    );
}
