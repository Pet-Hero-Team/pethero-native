import { SafeAreaView, Text } from 'react-native';

export default function SignupScreen() {
    return (
        <SafeAreaView className="flex-1 bg-neutral-50">
            <Text className="text-4xl font-extrabold text-neutral-900 mt-8">Signup</Text>
        </SafeAreaView>
    );
}
